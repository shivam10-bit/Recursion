# TODO

- [x] Identify options to commit without typing full git commit message
- [x] Check existing git version and repo hook directory
- [ ] (If user wants) Create `.git/hooks/commit-msg` to auto-fill message when empty

